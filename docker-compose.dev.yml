version: "3"
services:
  node-boilerplate:
    container_name: node-boilerplate
    build:
      context: .
      args:
      - ENV=${ENV:-dev}
      dockerfile: Dockerfile
    entrypoint: /bin/ash -c "./start.sh dev"
    environment:
      - ENV=dev
      - ENV_CODE=dev
